# Dashboard Example

This example is broader than `examples/minimal` and exists to anchor future parser, semantic, and backend work against a more realistic screen shape.

It is an aspirational fixture for the active slice. It intentionally exercises out-of-scope constructs and must not be treated as a passing validation target, starter-template baseline, or backend snapshot input until the ratified language contract expands.
