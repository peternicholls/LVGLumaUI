# Layout fixture — normative

Exercises Row/Column layout, explicit width/height, a hex-shaped id selector,
and a Button without children or a callback. The LVGL integration test builds
this fixture with C warnings treated as errors and inspects its object tree.
