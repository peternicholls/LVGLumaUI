# Fixtures

Shared fixture data can live here when examples are too large or too coupled to reuse directly.

For now, the workspace primarily reuses files under `examples/` as golden authored-source inputs.
